version: '3.8'

services:
  # PostgreSQL для всех сервисов
  db:
    image: postgres:14  # Изменено с latest на 14
    restart: always
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: adminadmin  # Изменено с postgres на adminadmin
    volumes:
      - postgres_data:/var/lib/postgresql  # Убрали /data
    ports:
      - "5432:5432"
    networks:
      - microservices

  # CRUD Service
  crud_service:
    build: ./crud_service/mysite  # Добавили /mysite
    command: >
      bash -c "python manage.py collectstatic --noinput &&
               python manage.py makemigrations &&
               python manage.py migrate &&
               gunicorn -w 2 -b 0.0.0.0:8001 mysite.wsgi:application"  # Уменьшили workers до 2
    volumes:
      - ./crud_service/mysite:/app
      - crud_static:/app/static
      - crud_media:/app/media
    expose:
      - "8001"
    depends_on:
      - db
    environment:
      - POSTGRES_DB=crud_drf
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=adminadmin
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
    env_file:
      - ./crud_service/.env
    networks:
      - microservices
    restart: always
    mem_limit: 512m  # Ограничение памяти

  # JWT/Auth Service
  jwt_service:
    build: ./jwt_service/mysite  # Добавили /mysite
    command: >
      bash -c "python manage.py collectstatic --noinput &&
               python manage.py makemigrations &&
               python manage.py migrate &&
               gunicorn -w 2 -b 0.0.0.0:8002 mysite.wsgi:application"  # Уменьшили workers до 2
    volumes:
      - ./jwt_service/mysite:/app
      - jwt_static:/app/static
      - jwt_media:/app/media
    expose:
      - "8002"
    depends_on:
      - db
    environment:
      - POSTGRES_DB=micro_jwt
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=adminadmin
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
    env_file:
      - ./jwt_service/.env
    networks:
      - microservices
    restart: always
    mem_limit: 512m  # Ограничение памяти

  # Cart Service
  cart_service:
    build: ./cart_service/mysite  # Добавили /mysite
    command: >
      bash -c "python manage.py collectstatic --noinput &&
               python manage.py makemigrations &&
               python manage.py migrate &&
               gunicorn -w 2 -b 0.0.0.0:8003 mysite.wsgi:application"  # Уменьшили workers до 2
    volumes:
      - ./cart_service/mysite:/app
      - cart_static:/app/static
      - cart_media:/app/media
    expose:
      - "8003"
    depends_on:
      - db
    environment:
      - POSTGRES_DB=cart_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=adminadmin
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
    env_file:
      - ./cart_service/.env
    networks:
      - microservices
    restart: always
    mem_limit: 512m  # Ограничение памяти

  # Nginx Gateway
  nginx:
    build: ./nginx
    ports:
      - "80:80"
      - "8080:8080"  # CRUD
      - "8081:8081"  # JWT
      - "8082:8082"  # Cart
    volumes:
      - crud_static:/app/crud_static
      - crud_media:/app/crud_media
      - jwt_static:/app/jwt_static
      - jwt_media:/app/jwt_media
      - cart_static:/app/cart_static
      - cart_media:/app/cart_media
    depends_on:
      - crud_service
      - jwt_service
      - cart_service
    networks:
      - microservices
    restart: always
    mem_limit: 256m  # Ограничение памяти

volumes:
  postgres_data:
  crud_static:
  crud_media:
  jwt_static:
  jwt_media:
  cart_static:
  cart_media:

networks:
  microservices:
    driver: bridge